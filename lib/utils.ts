// shadcn components import `cn` from "@/lib/utils". This project's cn lives in
// "@/lib/cn"; re-export it here so shadcn-style components (and the shadcn CLI)
// resolve the expected path without duplicating the implementation.
export { cn } from './cn';
